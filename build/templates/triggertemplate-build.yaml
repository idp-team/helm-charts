---
apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerTemplate
metadata:
  name: run-build-{{ .Values.app.name }}
spec:
  params:
    - name: git-revision
      description: The git revision
      default: {{ .Values.git.branch }}
    - name: git-repo-url
      description: The repo url
    - name: output-image
      description: reference of the image that will get created
      default: ''
    - name: namespace
      description: where the tekton project is installed
      default: {{ .Values.app.namespace }}
    # - name: cosignkey
    #   description: cosign key to sign and verify signatures
    #   default: ''
  resourcetemplates:
    - apiVersion: tekton.dev/v1beta1
      kind: PipelineRun
      metadata:
        name: $(uid)
        labels:
          backstage.io/kubernetes-id: {{ .Values.app.name }}
      spec:
        params:
          - name: source-repo
            value: $(tt.params.git-repo-url)
          - name: source-branch
            value: $(tt.params.git-revision)
          - name: output-image
            value: $(tt.params.output-image)
          - name: component-id
            value: {{ .Values.app.name }}
          - name: namespace
            value: $(tt.params.namespace)
          # - name: cosignkey
          #   value: $(tt.params.cosignkey)
        pipelineRef:
          name: build-pipeline-{{ .Values.app.name }}
        serviceAccountName: pipeline
        workspaces:
          - name: source-folder
            volumeClaimTemplate:
              spec:
                accessModes:
                  - ReadWriteOnce
                resources:
                  requests:
                    storage: 1G
          - name: maven-settings 
            emptyDir: {} 
          - name: docker-config 
            secret:
              secretName: acasas-docker-creds
         

          
